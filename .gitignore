node_modules/
dist/
.npm-cache/
.tmp-build*/
*.local
.DS_Store
Thumbs.db

# 编辑器
.vscode/*
!.vscode/extensions.json
.idea/

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 浏览器调试产物（Edge 调试目录，体积巨大且含锁文件，绝不入库）
.edge-dbg*/
.edge-debug/
.edge-test/
.edge-*/

# WorkBuddy 本地工作数据（记忆 / agent 文件等，按用户隔离，不入库）
.workbuddy/

# 旧版遗留（已弃用，不入库）
public/legacy/

# 仅排除仓库根级的调试/构建辅助脚本（诊断、截图、golden 回归样本等，非项目交付物，不入库）
# 注意：必须是 /tools/（前导斜杠=仅匹配仓库根），不能写 tools/，否则会误伤 src/tools/（核心路由页面）
/tools/

# 测试用 Word 样例（体积大、非源码，仅本地回归用，不入库；designer-json 参考输出保留）
samples/word/
